
extends Camera
onready var castle = get_node("/root/stage1/Spatial/edo")
var offset = Vector3(3.5,0,-15)

func _process(delta):

	#translation.x += sin(deg2rad(45))*delta
	#castle.scale +=  Vector3(0.001,0.001,0.001)
	#look_at(castle.translation + offset,Vector3.UP)
	pass
